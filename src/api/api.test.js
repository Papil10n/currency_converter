// import {ExchangeRatesAPI} from "./api";

describe("API testing", () => {

    test('test1', async () => {
        // const data = await ExchangeRatesAPI.getBase("USD");

        // expect(ExchangeRatesAPI.getBase).toBeCalledTimes(1)
    });

    test('test2', async () => {
        // const data = await ExchangeRatesAPI.getCurrenciesToUsd();

        // expect(ExchangeRatesAPI.getCurrenciesToUsd).toBeCalledTimes(1)
        // expect(Object.keys(data).length).toBe(4);
    });

});