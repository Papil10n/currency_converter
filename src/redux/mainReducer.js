// imports

// action mode

// initial state
let initialState = {
    currencies: []
};

// reducer
const mainReducer = (state = initialState, action) => {
    switch (action.type) {
        case "":
            break;
        default:
            return state;
    }
}

// action creator

// thunk creator

// exports
export default mainReducer;